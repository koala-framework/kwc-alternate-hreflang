<!-- alternateHreflang -->
{% if links %}
<link rel="alternate" hreflang="{{ currentLanguage }}" href="{{ currentLink }}" />
{%  endif %}
{% for link in links %}
<link rel="alternate" hreflang="{{ link.language }}" href="{{ link.url }}" />
{% endfor %}
<!-- /alternateHreflang -->
