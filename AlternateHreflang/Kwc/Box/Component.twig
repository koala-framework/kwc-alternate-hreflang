<!-- alternateHreflang -->
<link rel="alternate" hreflang="de-AT" href="{{ currentLink }}" />
{% for link in links %}
<link rel="alternate" hreflang="{{ link.language }}" href="{{ link.url }}" />
{% endfor %}
<!-- /alternateHreflang -->
